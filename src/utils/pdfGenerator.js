// utils/pdfGenerator.js
export const generatePDF = (projectData, userDetails) => {
  console.log("Simulating PDF generation. Project data:", projectData);
  console.log("User details:", userDetails);

  alert(
    "PDF generation feature is not implemented yet. Project data logged to console."
  );
};
